class TimingConstants {
  static const fadeInDuration = Duration(milliseconds: 2800);
  static const displayDuration = Duration(milliseconds: 8000);
  static const fadeOutDuration = Duration(milliseconds: 700);
  static const cycleDuration = Duration(milliseconds: 11500);
  static const backgroundPulseDuration = Duration(seconds: 40);
  static const pulseDotDuration = Duration(milliseconds: 2500);
}
